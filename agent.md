# Agent Development Log - Super A'Can Web Emulator 🤖

## 📅 日期：2026-05-15 ~ 05-17

## 🎯 任務概述
針對 Super A'Can Web 模擬器進行了深度的大改版，主要集中在**行動裝置（手機/平板）適應性**、**虛擬手把（Virtual Pad）實作**、**介面極簡化與 RWD 最佳化**，以及為特定遊戲提供專屬的**音訊死循環修復 (SndFix)**。

---

## 🛠️ 詳細修改紀錄

### 1. 音訊「喪屍循環」死循環修復
*   **問題**：特定遊戲（如《福爾摩沙大對決》、《嘻遊記》、《音速飛龍》等）在 Web 模擬時會產生持續不斷的刺耳噪音。
*   **解決方案**：
    *   編譯了專屬的 `mamesupracan-sndfix.wasm` 核心。
    *   引入了 UMC 6619 IRQ 修正與 ADSR 緩衝衰減機制。
*   **前端整合**：在 `loader.js` 中加入了遊戲判斷，當載入上述特定遊戲時，會自動切換至 `sndfix` 核心，其餘遊戲則維持原汁原味的標準核心。

### 2. 介面視覺與 Logo
*   **GitHub 連結**：在右上角語系切換旁，新增了圓形的 GitHub SVG Logo，並連結至開源倉庫，參考了 `a2gsemu-ia` 的風格。
*   **手把 Logo**：在虛擬手把的 Select/Start 按鈕上方，放置了 A'Can 經典的 `welcome-logo.jpg`，並調整了間距與負外距，使其靠上呈現。

### 3. 虛擬手把 (Virtual Pad) 實作
*   **外觀與配色**：
    *   A 鈕：深藍色 (`#0033aa`)
    *   B 鈕：亮黃色 (`#ffea00`)
    *   X 鈕：綠色 (`#28a745`)
    *   Y 鈕：紅色 (`#dc3545`)
    *   方向鍵：藍色 (`#007bff`)
    *   L/R 鈕：淺藍色 (`#60a5fa`)，並具有肩鍵（Shoulder）的外觀。
*   **鍵位對應與交換**：根據使用者習慣，將 A 與 B、X 與 Y 的物理位置進行了對調，以符合直覺。
*   **事件模擬**：透過發送 `keydown` 與 `keyup` 鍵盤事件（如 Control、Space、Alt、Shift 等），完美模擬 MAME 的原生鍵盤輸入。

### 4. RWD 與抽屜式互動設計
*   **電腦/平板模式（抽屜設計）**：
    *   在畫面底部實作了可拉出的「手把抽屜」（`#controllerDrawer`）。
    *   點擊箭頭可滑出/收合手把。
    *   **防遮擋機制**：當抽屜拉起時，Canvas 畫面會自動等比例縮小 (`max-height: calc(100vh - 460px)`)，確保遊戲畫面絕不被手把遮擋，並靠頂對齊 (`object-position: top center`)。
*   **手機模式（常駐設計）**：
    *   取消抽屜動畫與定位，手把改為**常駐顯示**於遊戲畫面下方。
    *   利用 Flex 佈局自動分配剩餘空間，使左側遊戲清單等 Frame 的底部剛好切齊在虛擬手把的上方，防止底部遊戲被壓到。
*   **極簡「嵌入式」標籤**：
    *   將手把拉環與左右 Frame 的切換開關一併極簡化。
    *   手把標籤縮小至 `40px * 16px`，並嵌入在上邊緣；Frame 開關縮小至 `16px * 40px`，並維持同一水平高度。

### 5. 行動裝置防呆與優化
*   在手機模式下，自動隱藏「原生 2x」與「全螢幕」按鈕。
*   手機模式下，預設收合右側資訊欄。
*   選取遊戲後，左側側邊欄會自動收回，騰出空間給遊戲畫面。
*   手機模式下隱藏 `DEBUG LOG (F2)` 的標籤。

### 6. UI/UX 極致打磨與細節優化
*   **視窗縮放智慧防呆**：
    *   實作了跨裝置 `resize` 監聽。當視窗從桌機尺寸縮小至手機尺寸（<= 768px）且遊戲進行中時，會自動將畫布切回 **Fit to Screen** 模式，防堵變形。
    *   同時，會**自動收合**左側遊戲清單與右側資訊欄，確保手機畫面的簡潔。
    *   在抽屜拉起或收合時，亦會強制重設 Canvas 的屬性解析度，逼 MAME 重新計算完美的畫面比例。
*   **「嵌入式」拉環標籤與視覺強化**：
    *   **動態嵌入**：當手把抽屜或 Debug Log 被拉開（Active）時，拉環會自動從 `top: -24px` 移至 `top: 0`，完美嵌入在頂部邊緣，且圓角從上方反轉至下方，極具現代感。
    *   **左右 Frame 連動**：左右側邊欄在展開時，切換按鈕也會自動「縮進」面板內部邊緣，防止突兀刺出。
    *   **視覺強化**：將拉環尺寸從 `40x16` 放大至 `60x24`，字級放大至 `16px`，並將顏色提亮為純白，徹底解決在暗色模式下「保護色隱形」的問題。
    *   **Debug Log 抽屜改造**：
    *   比照手把抽屜，將拉環極簡化為僅顯示箭頭的小標籤，並將 `DEBUG LOG (F2)` 的文字挪至抽屜內容的第一行，使介面語彙趨於統一。

### 7. 本地端極致優化與純 JS 準備腳本 (2026-05-16 ~ 05-17)
*   **純 JS 準備腳本 (`prepare.js`)**：
    *   廢棄了 `prepare.bat` 與 `prepare.sh`，將所有邏輯（下載 BIOS、大禮包、自動解壓、更名）統一用 Node.js 實作。
    *   解決了 Archive.org 重複下載的問題，會檢查 `boomzoo.bin` 是否存在來決定是否跳過。
*   **MyAbandonware 爬蟲與防過期機制**：
    *   實作了「兩階段潛入」機制，先訪問遊戲頁面獲取 Cookies，再帶上 Cookies 請求 `/download/[id]` 取得真實載點。
    *   支援追蹤 `303` 轉址，並解決了 HTTP 498 Token 過期問題。
*   **UI 狀態持久化 (LocalStorage)**：
    *   將「影像渲染模式（硬體/軟體）」、「畫面縮放模式（原生2x/適應螢幕）」與「語系偏好」全部寫入 `localStorage`，重啟或重整網頁不失憶。
*   **英文化與細節調整**：
    *   補齊了渲染按鈕與 Toast 提示的英文翻譯。
    *   應要求將 `Reload` 按鈕文字改為 `Restart`。
*   **資料去敏與 UI 細節打磨 (2026-05-17)**：
    *   **`games.js` 去敏**：移除了 `url` 與 `file` 欄位，避免直接暴露載點。
    *   **`loader.js` 自適應**：改用 `game.id` 判斷是 `.bin` 還是 `.zip`，不再依賴 `url` 欄位。
    *   **UI 修正**：拿掉了遊戲資訊面板中 `ROM檔:` 的顯示，解決 `undefined` 的問題。
    *   **彈跳視窗在地化**：修復了切換遊戲與按 ESC 退出時，對話框寫死中文的問題，完美支援英文化。
*   **音訊核心手動切換與清單標示 (2026-05-17)**：
    *   **新增音訊切換按鈕**：在 UI 上方新增了 `Sound: Auto/Original/Fixed` 按鈕，允許玩家手動切換音訊核心。
    *   **清單動態標示**：在左側遊戲清單中，為每款遊戲補上了 `Sound: Auto = Fixed` 或 `Sound: Auto = Original` 的詳細標示，並支援語系切換。
    *   **UI 語系細節優化**：連動了按鈕的 `title` 屬性 (Tooltip)，使其完全支援中英文切換。
*   **ROM 檔案智慧相容與跳過下載 (2026-05-17)**：
    *   **`loader.js` 雙格式相容**：支援優先讀取 `.bin`，失敗時自動降級讀取 `.zip`，並自適應切換 MAME 載入參數（`-cart` 或 Software List）。
    *   **`prepare.js` 智慧跳過**：檢查 9 款 Archive.org 遊戲是否已存在（支援 `.bin` 或 `.zip`），若全數存在則跳過大檔下載。
*   **MAME 自動跳框防 Lag 整合 (2026-05-19)**：
    *   **問題**：在遊玩《武將爭霸》或大量的動態 Sprite 遊戲時，因瀏覽器 CPU 單核繪圖或雙 CPU 同步性能吃緊，模擬速度可能掉到 60%–90%，導致聲音 choopy/慢動作。
    *   **解決方案**：在 `loader.js` 中將 `-autoframeskip` (自動跳框) 參數加入 MAME 的預設啟動參數。當執行速度因負荷過重低於 100% 時，模擬器會自動精準捨棄渲染極少量的幀，以確保**遊戲速度始終維持在 100% (60FPS 體感運行)** 且**音訊完全流暢且不中斷**，徹底解決了慢動作lag的問題。
*   **虛擬手把支援斜對角按鍵 (2026-05-19)**：
    *   **問題**：原本的虛擬手把方向鍵為十字型，難以在手機上觸發斜對角組合鍵（如上+右）。
    *   **解決方案**：
        *   將 `.dpad` 佈局從 Flex 改為 3x3 CSS Grid。
        *   在四個角落新增隱藏的透明按鈕，並支援 `data-keys` 與 `data-codes` 屬性以同時發送多個按鍵事件。
        *   優化 JS 觸控反饋與 CSS 權重，確保斜對角區域不顯示預設藍色，維持十字鍵視覺完整性，並提供微弱的點擊回饋。

---

## 📅 後續追蹤
*   持續觀察在不同手機瀏覽器（iOS Safari / Android Chrome）上的觸控靈敏度與音訊延遲。
*   未來若有更多 MAME 核心的驅動更新，將同步更新此典藏庫。

---

## 📚 附錄：MAME WASM 核心編譯與效能突破（源自 MameWasm 紀錄）

為了達成 100% 的運行速度與相容性，底層的 MAME WASM 核心也進行了極具侵略性的修改。以下節錄自 `MameWasm` 專案的開發紀錄：

### 1. 自訂目標與多 ROM 支援 (2026-05-14)
*   **獨立驅動編譯**：創建了 `custom_targets/supracan.lua`，修復了 Genie 中的 M68000 核心命名問題 (`M680X0`)，證明複雜主機也能編譯為輕量化的獨立 WASM。
*   **測試載入器升級**：支援同時選取多個 ZIP（如 BIOS + 遊戲）並掛載至 `/roms`，並支援動態 JS 載入與 CLI 參數（`-cart`）。

### 2. 全速 Super A'Can 效能大突破 (2026-05-15)
*   **量子平衡 (Quantum Balancing)**：拔除耗能的 `perfect_quantum`，改用平衡的 `6000Hz` 同步率，減少 20% 的 CPU 同步開銷。
*   **Sprite 智慧裁切引擎**：為螢幕外的 Sprite 加入 Early-Exit 邊界檢查，大幅減少多 Sprite 遊戲（如《棒球》）的冗餘運算。
*   **渲染扁平化與智慧髒標記**：將剪裁邏輯移出最內層迴圈，並優化 `vram_w` 僅標記必要區域，減少 VRAM 寫入開銷。
*   **極限編譯管線**：啟用 `-O3`、`-flto`、`-msimd128` (SIMD) 以及 `emmalloc` 記憶體分配器，並移除 Exception 開銷。
*   **結果**：在不開啟跳框（Frameskip）的情況下，於瀏覽器中達成原生 60 FPS 滿速運行，且音訊流暢不中斷。
